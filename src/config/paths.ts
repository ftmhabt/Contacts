import path from "path";

export const FILE_PATH: string = path.join(
  __dirname,
  "../../data/contacts.json"
);

export const CATEGORIES_FILE_PATH: string = path.join(
  __dirname,
  "../../data/categories.json"
);
